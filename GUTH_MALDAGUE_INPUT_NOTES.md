# Guth–Maldague 的具体外部输入

`CircleDivisor/GuthMaldagueInput.lean` 中的 `GuthMaldagueInput` 是完整写出的外部定理命题，**不是已经证明的定理，也没有被声明为 Lean 公理**。后续主证明须显式接受 `hGM : GuthMaldagueInput`。

来源：[Guth–Maldague, arXiv:2206.01093v2](https://arxiv.org/html/2206.01093v2)，Theorem 2、§5 的 (29)、圆锥 frame 与包络定义，以及 §2 的 (13)。采用本稿需要的正锥面特例和 §5 的径向截断 `1/2 ≤ ξ₃ ≤ 2`。原文引言先用另一固定径向上界；这里没有将两套数值混写。

输入实际定义了 `R=4^n`、`s=2^(-j)`、圆柱角度与半开角区间、`s²` 邻域里的角帽、锐 Fourier 投影、fine-cap 包含关系、原文 `c,t,n` frame、映至单位立方体的坐标、对应的整数平移格和包络、具体 product-decay 权重、局部质量、同尺度活跃粗帽数，以及通过 `ENNReal` 表示的非负可数包络求和。阈值和右侧共同使用同一个常数，常数和“大 R”门槛在所有函数与幅度之前量化。

已证明辅助一致性：frame 内积、包络坐标对 synthesis 的左逆、平移格的包络覆盖、基础权重的连续可积与处处正值、归一化常数严格正、权重积分一、质量非负、活跃粗帽数不超过 `2^j`、由真实活跃数阈值推出 `α²s²` 门槛。

输入采用 `tsupport (𝓕 f) ⊆ coneNeighborhood (1/R)`。这有意保留通常 Fourier 支撑的闭包含义：`Function.support ⊆` 一个开邻域本身不足以推出相同的 `tsupport` 包含关系。应用时应使用已有 cutoff 支撑的严格余量处理边界。

输入没有包含本稿的局部密度估计、整数正交性、四元组计数或能量界。仍须在本稿内部连接：固定映射与旋转后的实际 Schwartz 有限指数和、整个原子对具体角帽的包含/不交关系、本文包络参数和源定义的关系，以及将这里的非负可数和用于层蛋糕积分时所需的可测性、可积性和求和步骤。具体包络体积的 Jacobian 公式与权重有界重叠尚未在本文件证明；这里的体积始终定义为实际 Lebesgue 测度，未用一个自由输入数值替代。
