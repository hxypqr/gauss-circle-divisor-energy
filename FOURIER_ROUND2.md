# 第二轮：实际 Fourier 局部化与整数正交性

文件：`CircleDivisor/FourierLocalization.lean`。使用 `EuclideanSpace ℝ (Fin 3)`、Mathlib 的 Bochner Fourier 变换与 Schwartz 空间。

## 已证明

- 局部化指数原子的二阶、四阶积分，分别等于共同权重 `|χ|²`、`|χ|⁴` 的 Fourier 变换在差频率处的值。
- 利用 Mathlib 的卷积支撑定理，证明 Schwartz 函数乘积的 Fourier 支撑球半径相加；复共轭使 Fourier 变换共轭反射。由此实际证明 `supp FT(|χ|⁴) ⊆ B(0,4c)`，并证明第四权重可积。
- `χ(z/P)` 的 Fourier 缩放公式、支撑半径缩小为 `c/P`，以及第四权重积分恰为 `P³` 乘固定积分。
- 对稿件的实际三维频率 `(l/L, l√k/(L√K), kl/(KL))`，非零四阶积分严格强迫两个整数平衡方程；之后可丢弃平方根坐标限制。每项积分范数有统一上界。
- 上述结果接入旧 `FourierEnergy`，消除了其逐项可积、正交零值和范数界假设，得到任意有限配对族、任意模长不超过一的复系数的实际能量计数上界。
- `exists_scaled_energy_cutoff` 直接调用 `SchwartzCutoff.exists_cutoff`，给出固定 cutoff，且对所有 `K≥1,L>0`、配对族和系数给出除以 `(KL)³` 的归一化能量界；其结论不再以任何 Fourier 支撑或正交性断言为输入。
- 实际锐 Fourier 投影的有限和恒等式：若整个原子支撑球在扇区内则完整保留，在扇区外则完整删除。结果为 `sharpProjection_modulated_sum`。

## 本文件没有证明的几何和包络步骤

`sharpProjection_modulated_sum` 仍明确要求：每个原子的整个 Fourier 球包含于选定扇区，或与该扇区不交。这需要把两套角网格的实数间距论证连接到实际变换后的三维圆锥角坐标，包括固定线性映射 `A`、角度对频率扰动的统一 Lipschitz 界和径向截断。不能仅凭本文件的投影恒等式就声称整个 Lemma 2.1 已经完全证明。

本文件也未把具体包络矩阵 `B_U`、固定权重 `∏(1+u_j²)^(-100)`、任意平移中心及 χ 的导数统一估计连接起来。这是 Lemma 2.2 的实际核衰减证明，以及 Lemma 3.1 的格点邻居数求和仍须核验的接口。

## 对 §2.2–§3.1 的数学复查

未发现上述 Fourier 支撑、整数正交性、邻居密度或核衰减论证中的可定位反例或数学矛盾。`|χ|⁴` 的 `4c/P` 支撑及 `P³` 归一化此前是形式化空缺，现在已经实际证明。

已逐步核对：由两项适应坐标约束可推出 `|t-t'| ≲ 2^j/(Ps)`，继而得到 `|k-k'| ≲ 2^j/(Ls)`；另一坐标给出 `|l-l'| ≲ 2^j/(Ks²)`。在 `K≥L` 和 `s≳(KL)^(-1/2)` 下，`sK≳1`，所以粗扇区频率数确可写为 `O(sKL)`。核估计需要的是 χ 各阶导数有界、`B_U/P` 算子范数一致有界，以及固定权重各阶导数可积；这些条件足以使积分分部常数不依赖平移中心。

形式化第二阶完全正交时要明确保留 `l≠0`：不同 `k`、同一个非零 `l` 才由第三坐标分离。原稿 `l∼L≥1` 已满足此条件，故这不是原稿 gap；只是不能将此步骤推广为任意整数 `l` 的无条件单射断言。

对 Guth–Maldague 原定理的具体权重、尖锐投影及尺度定义是否完全一致，需要由外部接口审计继续核对；这里没有把这项核对替换为假设正确。
