# 第二轮形式化与疑点审计

对象：Xiyu Hu，*Amplitude-dependent first spacing and the circle and divisor problems*，
2026-09-06，21 页。对应指数
`(33333 - 8 * sqrt 64515) / 99856 = 0.313461542101…`。

**结论：本轮补出了多段实际分析与算术证明，但尚未证明第一间距定理 1.2，
也尚未证明圆／除数主定理 1.1。没有把缺失的内部结论改列为外部输入。**

## 已确认需要修订的数学表述

### §5.1 的“充分条件”必须补上适用范围

若把该节的 Case B 条件理解为足以保证 `R ≲ H`，则表述过强。
取 `H=1, M=T^(1/2)`，T 充分大时满足该节 Case B 两条上界，但

```
N_B = T^(23/80) (log T)^(969/5600),
R ≍ T^(17/160) (log T)^(-969/11200),
R/H → ∞.
```

于是不存在 `R≤Q≤3H` 的可用分母范围。这不是 mathlib 的问题。
[Li–Yang 的 Remark 4.5](https://arxiv.org/html/2308.14859v2) 也明确提醒
`R≤H` 不总成立，失败范围用初等估计处理。

稿件 §6.3 另在 hard range 证明了 `1≪R≪H≪N≪M`，因而这个问题目前定位为
**§5 的输入适用性表述和 §6 的调用条件需要显式连接**，不是主定理的已知反例。
建议把 §5 改为“以下 large-sieve 结论在所构造 block 满足所列分离条件时成立；
本文的 hard range 将在 §6.3 验证这些条件”。

代码没有把错误的全称版本写入外部输入。`LiYangInput` 保留了实际条件
`D≤R, D*R≤H, 64*C*H≤N, D*N≤M`。
本轮 `BlockSeparation.lean` 进一步证明：两种 block 模型都有统一的 `1/1000`
指数裕量，且实际 N/R 比较中的固定因子可由同一个充分大 T 阈值吸收。
`LogScale.lean` 已把两种真实 block 长度精确写成相应的 T 幂，并证明
`log(log T)/log T → 0` 及最终落在 `[0,1/100]`；这些对数事实不再需要外部假设。
尚需把外部实际选出的 N/R、对数模型与这些引理完成全链装配。

## 已排除或已补齐的疑虑

| 位置 | 本轮结果 | 是否还构成数学疑点 |
|---|---|---|
| §2.1 共同 Schwartz cutoff | 实际构造 Fourier 支撑任意小、指定紧球上模长 ≥1/2 的函数；没有声称物理空间存在平顶 | 此存在性已形式化解决 |
| §3.3 整频率正交性 | 证明 `|χ|⁴` 的支撑扩大至 `4c`、缩放至 `4c/P`、P³ 积分因子，再推出两项整数平衡条件 | 此前的正交性输入已消除；仍须接 envelope 去权重 |
| 两套角网格 | 任意实角的 δ/4 避界与 δ/8 扰动同单元已证；实际锐投影保留整个原子也已证 | 尚缺三维极角扰动与真实锥扇区的实例化；未发现反例 |
| 变化的 q | 对真实 coneSum 和 coneMoment 用有限下取网格，证明逐 q 版本与紧区间一致版本等价 | 第一间距的此项疑虑已解决；Li–Yang 优化端仍须实际接合 |
| Li–Yang 的周期盒 | 原文 [-1,1]² 与本文 [0,1]² 的归一化积分相等由 Fubini 与整数周期性证明；小 η 盒也接到了真实 coneMoment | 归一化错配已解决 |
| 真实倒数相位 | 实际证明 a/(u+s)+b 的前三导数、曲率行列式 -6a²/(u+s)^6 与统一 PhaseControl，覆盖圆／除数相位 | 此适用条件已形式化解决 |
| §7.2 除数归约 | 从真实 divisorSummatory 证明双曲线公式、锯齿和公式、统一余项 ≤12+8C | 已完成；C 只来自明确的经典调和数输入 |
| §7.3 圆归约 | 从真实 circleCount/ncard 证明两平方系数求和、字符双曲线、β 尾项、移位与端点抵消；余项 ≤23 | 已完成；未发现该节数学 gap |

## 最难且尚未完成的内部步骤

1. **真实几何与权重核（§2.2–3.1）。** 需要把 A 变换后的 Fourier 小球接到圆柱角帽，
   定义实际 envelope 矩阵 B_U，并从真实 `|χ(z/P)|² W_U(z)` 证明衰减，且所有导数常数
   对中心、尺度、K/L 一致。随后要将邻居界与衰减壳层求和组合成 M(s)、D(s)。
   现有通用 Schwartz/Fourier 库可用，但这组应用引理仍须编写。

2. **包络的可数求和与两类能量（§3.3–4）。** 要证明实际平移格权重有界重叠，
   将 envelope 局部能量求和后去权重，再接本轮全空间正交性和已证八整数计数。
   全等 k 的排除必须与真实 cross-ray 分裂保持对应；不能以一个抽象能量界代替。
   之后才能从 Guth–Maldague 输入推出实际超水平估计，再应用已有 layer-cake 和尺度界。

3. **Li–Yang 输入的实际调用（§5–6）。** 要完成 block 选择、真实参数与幂模型、
   端点权重、所有 Q 范围、log／取整／固定常数、有限 q 优化的连贯证明。
   目前很多组成引理已证，但没有一条完成的定理控制本文实际 reciprocalSum。

4. **Fourier 到锯齿和的装配（§7.1）。** 有限 Vaaler 起始不等式已证，且保留频率和的
   符号抵消。仍需具体系数的 BV／离散变差界、矩形指数和的部分求和、二进区间分解和
   ε 吸收。剩余目标已明确写成 `FinalAssembly.SawtoothBounds`，它是待证内部命题。

这些步骤尚未有 Lean 证明，不能据此判断原稿必有数学错误。本轮独立复查没有在上述
核衰减／局部密度论证中定位到反例；也没有完成足以认证其全部细节的形式证明。

## 外部输入的核对边界

- [Guth–Maldague Theorem 2、§5](https://arxiv.org/html/2206.01093v2)：
  已核对 sharp angular restrictions、允许的截锥范围、真实 polynomial-tail 权重和
  `#τ` 的含义。mass 阈值与 L²-average 阈值互换时，需要同时重选小指数及常数。
  本轮已写出完整具体的 `GuthMaldagueInput` 命题，含多尺度角帽、实际锐投影、frame、
  整数平移包络、权重、质量及 ENNReal 可数和；采用 `tsupport`。不再把“外部输入尚未
  写出”列为缺项；仍缺的是本文 χF₀ 对该输入的实例化和后续内部估计。
- [Li–Yang Lemma 4.4、Remark 4.5](https://arxiv.org/html/2308.14859v2)：
  已写实际输入及明确的分离前提，未把插入 Li–Yang 特定 spacing 后才出现的 (4.6)
  加入裸 large-sieve 输入。输入形式化不等于在 Lean 内重证文献定理。
- 经典 divisor、two-squares、β(1)、harmonic expansion 和 Vaaler 输入均保留具体公式、
  参数和端点约定，没有全局 `axiom`。

详细模块报告：`FOURIER_ROUND2.md`、`GUTH_MALDAGUE_INPUT_NOTES.md`、
`EXTERNAL_INTERFACE_AUDIT_ROUND2.md`、`DISCREPANCY_ROUND2.md`。
编译与审计结果见 `verification/STATUS.md`。

## 如何理解“mathlib 基础设施不足”

本轮已经用 mathlib 的真实 Schwartz/Fourier、卷积、Bochner 积分、测度换元、Fubini、
周期积分、渐近与有限计数基础设施推进了此前缺失的部分。困难主要在于尚须构造和证明
本文专用的应用层连接，并非这些对象无法表达。唯一明确定位的范围问题是上面的 §5.1；
它不能用库不完善来解释，也不能直接升级为主结论错误。
